# AI Usage Declaration

Declaring AI use does not reduce the grade. You must be able to explain and validate every submitted decision.

## Lab 04 — Architecture Foundation

| Tool | Activity | Prompt / purpose | How I validated the result | What I changed / rejected |
|---|---|---|---|---|
| Claude (Claude Code / Anthropic) | Explicación guiada de la arquitectura del starter (puertos/adaptadores, DIP, inyección de dependencias por constructor) | Entender por qué `BoardApplicationService` debe depender solo de la interfaz `BoardRepository` y nunca de `InMemoryBoardRepository`, y quién conecta ambas en tiempo de ejecución | Revisé el código real del starter junto con la IA (anotaciones `@Service`/`@Repository`, constructor de `BoardApplicationService`) y confirmé que ningún archivo hace `new InMemoryBoardRepository()` manualmente — la conexión la arma el contenedor de IoC de Spring | No aplica (fue explicación, no código) |
| Claude (Claude Code / Anthropic) | Implementación de `BoardApplicationService.createBoard/getBoard/replaceBoard` y resolución de los 2 TODOs de `InMemoryBoardRepository` (semántica de `save`, necesidad de copia defensiva) | Se pidió completar los 3 casos de uso del Application Service contra el puerto `BoardRepository`, y documentar las decisiones de diseño pendientes en el adaptador en memoria | Corrí `mvn test` en mi máquina y confirmé `Tests run: 4, Failures: 0, Errors: 0, Skipped: 0` antes de aceptar el código; también probé los 3 endpoints manualmente (`Invoke-RestMethod`: create, get, put) contra el servidor real levantado con `mvn spring-boot:run` | Se aceptó la lógica tal cual — no hubo cambios de fondo, solo tuve que corregir en qué carpeta (`src/main` vs `src/test`) quedó cada archivo, porque me confundí pegando el contenido |
| Claude (Claude Code / Anthropic) | Pruebas unitarias adicionales de `BoardApplicationService` (casos de `replace`) y pruebas de contrato REST nuevas (`BoardRestControllerTest`, con `TestRestTemplate`) | Cubrir el caso "Board inexistente" en `replace` (no solo en `get`) y demostrar el contrato REST completo (create 201, get 200/404, replace 200/404) con el servidor real levantado en un puerto aleatorio | Corrí `mvn test` y confirmé `Tests run: 9, Failures: 0, Errors: 0, Skipped: 0` | Se aceptaron tal cual tras confirmar que pasaban |
| Claude (Claude Code / Anthropic) | Documentación: `docs/architecture/api-contract.md` y `docs/ADR-001-repository-boundary.md` | Documentar el contrato REST realmente implementado (no uno "ideal") y justificar la decisión de separar el puerto `BoardRepository` del adaptador `InMemoryBoardRepository` | Comparé el contrato documentado contra las respuestas reales que obtuve por HTTP; para el ADR, confirmé personalmente que el comando `grep -r "import edu.eci.arsw.collabboard.infrastructure" src/main/java/.../domain src/main/java/.../application` no devuelve resultados en mi propio repositorio | La primera versión de `api-contract.md` no seguía la tabla exacta del template que trae el starter (`Method \| Resource \| Request \| Success response \| Error cases`) — se corrigió para respetar esa estructura en vez de una estructura libre |
| Claude (Claude Code / Anthropic) | Depuración de entorno local (Windows): `JAVA_HOME` mal configurado y un crash intermitente de la JVM al correr `mvn test` | Diagnosticar por qué `mvn test`/`mvn spring-boot:run` fallaban con errores no relacionados con el código (error de parseo de `cmd.exe`, "stack overflow" de la JVM, canal corrupto de Surefire) | Verifiqué en mi propia terminal que, tras reiniciar el computador, `echo $env:JAVA_HOME` mostraba el valor correcto y `mvn test` pasaba de fallar a `BUILD SUCCESS` | No aplica — fue troubleshooting de entorno, no una decisión de arquitectura ni de código del laboratorio |

## Lab 05 — Interactive Board

| Tool | Activity | Prompt / purpose | How I validated the result | What I changed / rejected |
|---|---|---|---|---|
| Claude (Claude Code / Anthropic) | Evolución del dominio: `ElementType.CONNECTOR`, `sourceId`/`targetId` en `BoardElement` y `Board.validateConnectors` | Agregar CONNECTOR con las invariantes de la guía (ambos ids obligatorios, distintos y apuntando a elementos existentes) sin tocar el controlador ni el servicio, y cubrirlas con pruebas | Corrí `mvn test` (`BoardConnectorTest` + las pruebas del Lab 04 actualizadas al nuevo constructor de 9 argumentos) y revisé que `BoardRestController` y `BoardApplicationService` no cambiaran | Se agregaron dos reglas que la guía no pedía: rechazar ids de elemento duplicados y prohibir `sourceId`/`targetId` en elementos que no son CONNECTOR. En un segundo commit se corrigió que un CONNECTOR podía apuntar a otro CONNECTOR (el contrato ya decía "non-connector elements" pero el código no lo validaba) y se añadió `shouldRejectConnectorReferencingAnotherConnector` |
| Claude (Claude Code / Anthropic) | Cliente web SVG separado en `BoardApiClient`, `BoardState`, `BoardView` y `BoardApp` (`app.js`) | Completar los `TODO LAB-05` del starter respetando la separación: `fetch` solo en el API client, estado local como única fuente de verdad, vista como proyección del estado, y loading/error/retry en la orquestación | Probé el flujo en el navegador con `mvn spring-boot:run` (crear, cargar, agregar, mover, conectar, eliminar, guardar y recargar); verifiqué con `grep -rn "fetch(" src/main/resources/static/js` que solo aparece en `board-api-client.js` | Se añadieron cosas que no venían en la primera propuesta: validar el `boardId` vacío antes de llamar a la red, deshabilitar los botones mientras hay una operación remota y eliminar los conectores que apuntaban a un elemento borrado para no guardar conectores colgantes |
| Claude (Claude Code / Anthropic) | Depuración del bug de `structuredClone` en `BoardState.snapshot()` | Encontrar por qué todos los botones fallaban al primer uso | Cualquier botón fallaba al primer uso. La causa: `remote.lastAction` es una función (closure) y `structuredClone` no puede clonar funciones. Después del cambio se volvió a probar en el navegador que los botones y Retry funcionaran | Se rechazó quitar `lastAction` del estado, porque Retry lo necesita. En su lugar, `snapshot()` clona todo lo demás y vuelve a asignar `lastAction` por referencia |
| Claude (Claude Code / Anthropic) | Corrección de 3 defectos del cliente encontrados al probar la entrega: los elementos TEXT no se podían seleccionar/mover/conectar/eliminar, el botón Retry no aplicaba el resultado, y cada error dejaba un `Uncaught (in promise)` en la consola | Revisar el flujo completo exigido por los criterios de aceptación en el navegador y arreglar lo que no cumpliera | Los tres se verificaron en el navegador: (1) `document.elementFromPoint` sobre un TEXT ahora devuelve el `<text>` y no el `<svg>`, y se logró seleccionar, mover, conectar (TEXT → CONNECTOR → RECTANGLE persistido por `PUT`) y eliminar un TEXT (al borrarlo desapareció también su conector y el rectángulo se conservó); (2) forzando un fallo de `fetch` y luego reintentando, el Retry cargó y renderizó el board correcto; (3) la consola ya no muestra el `Uncaught (in promise)`. `mvn test` sigue en 12 pruebas sin fallos | La causa del bug de TEXT era `.label { pointer-events: none; }` en `app.css`. Al quitarlo, un TEXT seleccionado se veía ilegible porque `.selected` le aplicaba un borde grueso, así que se agregó una regla aparte que lo resalta por color y grosor de letra. El Retry se arregló cambiando el contrato de `remote(label, action)`: ahora la acción hace la llamada **y** aplica el resultado al estado, así el reintento repite la operación completa |
| Claude (Claude Code / Anthropic) | Documentación del Lab 05: `docs/architecture/api-contract.md`, `docs/ADR-002-client-boundaries.md`, esta sección de AI_USAGE y evolución de `docs/architecture/diagrams.drawio` (ArchiMate + clases/módulos) | Documentar el contrato real con CONNECTOR y sus reglas, justificar la separación del cliente y actualizar los diagramas del Lab 04 para que muestren el cliente web y CONNECTOR | Antes de escribir el contrato, levanté el servidor y probé con `curl` cada regla (conector válido → 200 y `GET` lo devuelve; destino inexistente, mismo origen/destino, conector a conector, id duplicado, RECTANGLE con `sourceId` → 400 `INVALID_INPUT`). Comparé los nombres y dependencias de los diagramas con los imports reales de los módulos JS y con las clases Java | Al probar apareció algo que no se esperaba: un `type` desconocido (`CIRCLE`) o JSON malformado devuelve el cuerpo de error por defecto de Spring y no un `ApiError`. Se documentó como limitación conocida en vez de afirmar que todos los errores son uniformes |
| Claude (Claude Code / Anthropic) | Cierre de la entrega: README actualizado al Lab 05, índice de evidencia en `docs/architecture/README.md`, mover el `.drawio` a `docs/architecture/` y reemplazar el comentario `TODO LAB-04` de `BoardRepository` | Dejar el repositorio coherente con lo entregado: sin texto del starter, con la evidencia donde la piden las guías y con los criterios de aceptación y la pregunta de sustentación respondidos | Cada checkbox marcado en el README corresponde a algo verificado en esta sesión (`mvn test` con 12 pruebas, los 3 endpoints por HTTP, el flujo completo del cliente en el navegador, y `grep -rn "fetch(" src/main/resources/static/js` que solo devuelve los 3 llamados del API client). La respuesta de sustentación se contrastó con el código: ningún archivo hace `new InMemoryBoardRepository()` y el dominio no importa nada de `infrastructure` | Se rechazó marcar los criterios "por confianza": los del cliente se marcaron solo después de encontrar y arreglar el bug que impedía seleccionar/mover TEXT. El `.drawio` se movió con `git mv` para conservar el historial |
| Claude (Claude Code / Anthropic) | Preparación de la base para el Lab #6: `target/` y los `.DS_Store` fuera del control de versiones (`.gitignore` + `git rm --cached`) y arreglo de los campos `boardId`/`boardName`, que `refresh()` sobrescribía en cada render | Dejar el repositorio limpio antes de agregar WebSockets, y que los campos de la interfaz dejen de perder lo que el usuario escribe | Verificado en el navegador: escribir un id/nombre y hacer acciones locales (+ Rectangle, + Text) ya no los borra; un Load real sí refresca ambos campos con el board del servidor; un Load fallido conserva el id escrito y muestra Retry. Se repitió el flujo completo (crear, agregar, conectar, guardar, recargar) y `mvn test` siguió en 12 pruebas. Se confirmó que `target/` sigue en disco y solo salió del índice de Git | El primer intento comparaba el board anterior con el nuevo para decidir si sobrescribir los campos, y dejaba el nombre viejo tras un Load del mismo board. Se cambió por sincronizarlos solo cuando llega un board del servidor (New/Load/Save), que es cuando de verdad quedan obsoletos |

## Lab 06 — Real Time Collaboration

Filas 1 a 4: trabajo de Sofía (commits `42b9ebd` a `fb735bb`). Filas 5 a 8: trabajo de José sobre esa base.

| Tool | Activity | Prompt / purpose | How I validated the result | What I changed / rejected |
|---|---|---|---|---|
| Claude (Claude Code / Anthropic) | Implementación de la colaboración en tiempo real: contrato `BoardEvent` / `BoardEventType` / `BoardEventPayload`, `WebSocketConfig`, `BoardEventApplicationService` y `BoardWebSocketController` en el backend; `board-event.js`, `board-realtime-client.js`, `BoardState.applyEvent` y la integración en `app.js` en el cliente | Completar los TODO del Lab 6 respetando los límites de la guía: STOMP encerrado en un solo módulo del cliente, cada evento validado y aplicado por un servicio de aplicación antes del broadcast, y el callback STOMP convertido en una transición de `BoardState` que no toca el DOM | `mvn test`: 36 pruebas, 0 fallos. Son 24 nuevas (15 de `BoardEventApplicationServiceTest`, 5 de `BoardWebSocketControllerTest`, 4 de `BoardEventContractTest`) y las 12 de los Labs 4 y 5 sin cambios. Greps de frontera: `/topic/boards` solo aparece en `realtime/board-realtime-client.js`, `fetch(` solo en `api/board-api-client.js` y `convertAndSend` una sola vez, en `BoardWebSocketController` | La primera versión tenía dos defectos que se encontraron y corrigieron después (filas 2 y 3). La secuencia leer-modificar-guardar de `BoardEventApplicationService.apply` se dejó sin sincronizar a propósito: es el material del Lab #7, y así está dicho en el código, en el README y en el ADR-003 |
| Claude (Claude Code / Anthropic) | Depuración del formato de `occurredAt`: el transporte STOMP lo enviaba como número (`1790134084.565000000`) y no como el texto ISO-8601 que documenta `docs/event-contract.md` | Entender por qué el mismo `BoardEvent` tenía dos formatos de cable según el transporte que lo llevara, y corregirlo sin cambiar el contrato | La prueba nueva de formato de cable lo expuso (commit `6fa8ec2`), y `shouldWriteOccurredAtAsAnIsoInstantAndNotANumber` deja fijado el formato. Además se comprobó en el transporte real: en un frame STOMP `MESSAGE` capturado en el navegador desde `/topic/boards/{id}`, `occurredAt` llega como `"2026-09-25T00:15:00.568Z"` | La causa era que la capa de mensajería construía su propio `ObjectMapper`. Se corrigió en la configuración, no en el contrato ni en el cliente: `WebSocketConfig.configureMessageConverters` reutiliza el `ObjectMapper` que Spring Boot configura para REST y devuelve `false` para que los convertidores por defecto no se registren encima. Así un solo contrato tiene un solo formato de cable |
| Claude (Claude Code / Anthropic) | Revisión de fronteras del cliente: `app.js` armaba el texto `"/topic/boards/{id}"` para mostrar en pantalla a qué destino quedó suscrito | Reunir la evidencia del ADR-003 de que los destinos STOMP solo se construyen dentro de `BoardRealtimeClient` | El grep que se estaba preparando para el ADR (`grep -rn "/topic/boards" src/main/resources/static/js`) encontró la coincidencia en `app.js`. Después del cambio solo devuelve `realtime/board-realtime-client.js`, y la pantalla sigue mostrando `Subscribed to /topic/boards/{id}` (se ve en la captura del paso 8 de `docs/evidence/`, Browser C) | Aunque solo era un mensaje en pantalla, un topic escrito en `app.js` es un detalle de STOMP dentro del orquestador, justo lo que prohíbe la sección 5.5 de la guía. `BoardRealtimeClient` ahora expone `topic()` y `app.js` solo muestra lo que el módulo le devuelve |
| Claude (Claude Code / Anthropic) | Verificación funcional con tres ventanas: dos en el mismo `boardId` y una tercera en otro Board (sección 5.7 de la guía) | Comprobar los criterios de aceptación del Lab 6 en el navegador, no solo con pruebas unitarias | Sofía hizo la prueba a mano contra `mvn spring-boot:run`: crear, mover, conectar y eliminar se propagaron en las dos direcciones, la tercera ventana no recibió nada y una recarga recuperó el estado por REST. En la corrida grabada de la entrega (fila 6): A y B recibieron los mismos 7 eventos, el borrado en cascada llevó a los dos clientes de 3 nodos + 1 conector a 2 nodos + 0 conectores, y C recibió 0 frames `MESSAGE` | El ADR-003 decía que el borrado pasaba de "3 nodes + 1 connector" a "1 node + 0 connectors", cuentas que no cuadran: borrar un nodo de tres deja dos. Se reemplazó por los números de la corrida grabada, que se pueden revisar en las capturas y en `run-summary.json` |
| Claude (Claude Code / Anthropic) | Actualización de `docs/architecture/diagrams.drawio` al Lab 6: vista ArchiMate con la interfaz WebSocket/STOMP y diagrama de clases con las 6 clases Java y los 2 módulos JS nuevos | Que los diagramas cumplan la regla del equipo (cada caja es una clase o módulo real, cada flecha una dependencia real) y cubran lo que pide la sección 8 de la guía | Antes de dibujar cada caja se revisaron sus imports y firmas en el código. El XML se validó con un script (ids únicos, ninguna flecha hacia una caja inexistente) y las dos páginas se renderizaron con el visor oficial de draw.io para revisarlas a ojo: se corrigieron tres etiquetas que quedaban encima de los bordes de los grupos y el espacio sobrante de notas y leyendas | El traspaso pedía agregar a la vista ArchiMate solo la interfaz STOMP y `BoardEventApplicationService`; también se agregaron `BoardRealtimeClient` y `BoardWebSocketController`, porque sin ellos la interfaz nueva quedaba sin quién la usara ni quién la realizara. `WebSocketConfig` va sin flechas porque no referencia ninguna clase del proyecto, y no hay flecha de `BoardRealtimeClient` a `BoardEvents` porque ese import no existe: el sobre lo construye `app.js`. Se quitaron las negritas del Lab 5 para que la negrita signifique "agregado en el Lab 6" |
| Claude (Claude Code / Anthropic) | Evidencia de la demostración mínima (sección 9 de la guía): `docs/evidence/lab06/` con 10 capturas, el script que las genera y el conteo de frames por ventana | Capturar los 9 pasos con tres navegadores aislados de forma reproducible, con énfasis en las dos ventanas sincronizadas, el borrado en cascada y la tercera ventana en otro Board | `capture-lab06.js` (puppeteer-core sobre el Chrome instalado, tres contextos que no comparten cookies ni storage) espera a que cada cambio llegue a la otra ventana antes de capturar y registra cada frame WebSocket y cada respuesta de `/api/boards`. Las capturas se revisaron una por una. El log del servidor no muestra eventos rechazados ni descartados | La primera corrida produjo paneles vacíos: el script convertía mal a base64 el `Uint8Array` de la captura. Se detectó al revisar las imágenes y se corrigió. Todos los arrastres iban de B a A, y el criterio de aceptación habla de mover en A, así que el arrastre del paso 6 lo hace A. Se quitó el contorno de foco del lienzo en las capturas |
| Claude (Claude Code / Anthropic) | Documentación de cierre: sección Lab 06 de `docs/evidence/README.md`, filas del Lab 6 en `docs/architecture/README.md`, corrección del ADR-003 y esta sección de AI_USAGE | Dejar los documentos coherentes con lo entregado, de modo que el README, el ADR y los índices apunten a evidencia que existe | Cada número citado sale de `run-summary.json`, de `mvn test` o de un comando que se volvió a correr en esta sesión | Solo se declara lo que se puede verificar en el historial, el código o la corrida grabada. Las filas 1 a 4 describen el trabajo de Sofía según sus commits y el traspaso; no se inventaron propuestas de la IA que ella haya rechazado |
| Claude (Claude Code / Anthropic) | Corrección de una fuga de aislamiento en el cliente: una ventana en `LIVE: connected` que usaba **Load** o **New** con otro Board seguía suscrita al topic del Board anterior y aplicaba sus eventos sobre el nuevo | Se encontró al escribir `docs/live-demo.md`: la guía pide "abrir un Board diferente" en el paso 8, y la forma natural de hacerlo en una ventana ya conectada era justo la que fallaba | Se reprodujo contra la app antes de tocar el código: la ventana mostraba el Board Y con un rectángulo que era del X, mientras el servidor tenía Y vacío (un Save lo habría guardado en el Board equivocado). Después del arreglo, la misma reproducción da 0 elementos ajenos y `LIVE: disconnected`; cargar otra vez el mismo Board mantiene el canal; al reconectar, la ventana recibe los eventos de Y y no los de X; y la demo completa de 9 pasos volvió a pasar con los mismos números. `mvn test` sigue en 36 pruebas | Dos cambios en `app.js`: `closeLiveChannelIfBoardChanged` cierra el canal cuando New o Load traen otro Board, y `onEvent` descarta cualquier evento cuyo `boardId` no sea el del Board en pantalla, igual que ya hace el servidor. No se reconecta sola al Board nuevo: conectarse sigue siendo un paso explícito, como en el resto del cliente. Se agregó la función a la caja `BoardApp` del diagrama de clases y se volvieron a exportar los PNG |
